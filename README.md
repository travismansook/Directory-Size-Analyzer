# Directory-Size-Analyzer

• Built a Bash script that scans a directory, lists all files with their sizes, and calculates the average file size
• Parsed and processed ls output with awk/sed/cut to identify and report files larger than the average
